service cloud.firestore {
  match /databases/{database}/documents {
    //match /{document=**} {
      //allow read, write: if request.auth.uid != null;
   // }
   match /stripe_customers/{uid}/{allDocuments=**} {
     allow read: if request.auth.uid == uid;
   }
  }
}